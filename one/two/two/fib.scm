; @param n number the number for which to compute the fibonacci sequence
; n >= 0
(define (fib n)
  (cond ((= n 0) 0)
        ((= n 1) 1)
        (else (+ (fib (- n 1))
                 (fib (- n 2))))))
